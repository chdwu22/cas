<%= render 'shared/errors', obj: room %>
<%= form_for(room) do |f| %>
  <div class="field">
    <%= f.label "building" %>
    <%= f.number_field :building_id %>
  </div>
  
  <div class="field">
    <%= f.label :number %>
    <%= f.number_field :number %>
  </div>

  <div class="field">
    <%= f.label :capacity %>
    <%= f.number_field :capacity %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
